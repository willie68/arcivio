# Optional secrets (same structure as service.yaml). Merged on start.
# RSA signing key is generated automatically under data/idp/private.pem
# unless privateKey or privateKeyFile is set here:
#
# auth:
#   properties:
#     privateKey: |
#       -----BEGIN PRIVATE KEY-----
#       ...
#       -----END PRIVATE KEY-----
